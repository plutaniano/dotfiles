syntax on

" auto-indent
set ai

" show relative linenumbers
set number relativenumber

" highlight all search results instead of only showing the current match
set hlsearch

" show current column
set ruler

highlight Comment ctermfg=green
set backspace=indent,eol,start

